#ifndef _TREE_H_
#define _TREE_H_

#include <stdlib.h>
#include <stdio.h>
#include <limits.h>


#define MAX_INT(x, y) ((x) > (y) ? (x) : (y))
#define MAX_NUM 1000

typedef int myElement;

#endif